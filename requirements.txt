# Dependencies for the Vercel Python function api/tutor-api.py.
# Just the API package: its pyproject already depends on tutor-agent (via a
# [tool.uv.sources] editable path) and carries psycopg/httpx directly, and
# naming tutor-agent here a second time makes uv reject the lock as
# "conflicting URLs" (editable vs not).
tutor-api @ ./apps/api
