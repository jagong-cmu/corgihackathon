node_modules/
dist/
dist-ssr/
*.local
.DS_Store
*.log
.vite
.cache
# Broad on purpose: .env.bak / .env.save / .env.2 are all real things editors
# and shell one-liners create, and any of them can carry live credentials.
.env
.env.*
!.env.example
.gstack/

# Agent worktrees — nested checkouts, and they carry their own .env files.
.claude/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# uv.lock IS committed — it pins the agent's dependency graph.
